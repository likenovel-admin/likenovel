apiVersion: v1
kind: Service
metadata:
  labels:
    app: likenovel-admin
  name: likenovel-admin-service
spec:
  type: ClusterIP
  selector:
    app: likenovel-admin
  ports:
    - port: 80
      targetPort: 3002